<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">
      <%= 13 %>
    </strong>
    following
  </a>
  <a href="<%= followed_user_path(@user) %>">
    <strong id="followers" class="stat">
      <%= 12 %>
    </strong>
    followers
  </a>
</div>